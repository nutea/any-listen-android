# Copy to keystore.properties in the repo root. That file is gitignored.
# Do not commit the keystore or passwords.
storeFile=secrets/upload.jks
storePassword=replace-me
keyAlias=upload
keyPassword=replace-me
