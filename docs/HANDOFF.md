# 给 Hermes / 后续开发者的交接

请按 README 阅读计划，从 M0 开始。当前仅有文档，所有功能未实现。

1. 确认目标服务确为 v0.11.0-beta.1，获取摘要、匹配源码与手机型号。
2. 通过安全渠道取得域名与测试登录信息，绝不提交秘密。
3. 执行 API_DISCOVERY.md，保存脱敏证据和能力矩阵。
4. 可同步准备本地工程，但在协议未确认前只使用mock，不虚构HTTP路由。
5. M0通过后按M1–M5推进，每次提交说明完成任务ID、测试结果与剩余缺口。

当前无需新建服务器、LX同步服务或WebDAV。服务端升级和补丁不是本次自动授权范围。

若没有真机/凭据，继续能独立完成的基础工作，明确哪些门槛仍待输入；不能用模拟测试替代真实后台/离线验收。

第一份交付应为实际版本接口报告，而不是全套未经服务端验证的UI。
